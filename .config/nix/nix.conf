allow-dirty = true
# Stopgap until `nixos-rebuild switch` pins nixPath to the locked nixpkgs input.
# Default is 3600s, so every hour nix-direnv's `<nixpkgs>` eval re-fetches
# flake:nixpkgs over the network on the next `cd`, stalling the shell for seconds.
tarball-ttl = 604800
